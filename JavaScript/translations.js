/**
 * ВАЖНОЕ ИЗМЕНЕНИЕ
 * Файл переводов больше не используется для контента курсов.
 * Контент курсов будет представлен в отдельных версиях для каждого языка.
 */

// Файл сохранен для обратной совместимости
// Не переопределяем объект translations, если он уже существует
if (typeof translations === 'undefined') {
    window.translations = {};
}